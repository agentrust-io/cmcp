# Hardware validation runs

Repository-owned harnesses and evidence from live cMCP confidential-computing runs.

- `cmcp-azure-cvm-2026-07-20/` contains the captured Azure CVM evidence and nonce.
- `cmcp-in-tee-azure-2026-07-27/` contains the live harness and captured transcript.

Run a dated harness from its directory in an equivalent confidential-computing environment. Preserve captured evidence and add new runs as new dated directories rather than overwriting prior vectors.
