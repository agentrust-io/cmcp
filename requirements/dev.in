# Runtime + dev dependencies, mirroring [project.dependencies] and the dev
# extra. bandit and pip-audit are already in that extra, so the security-scan
# step installs from this same lock rather than fetching them again.
# Installed with --require-hashes before the local package goes in with
# --no-deps, because pip cannot hash-pin an editable install in the same
# invocation. Compile with:
#   uv pip compile requirements/dev.in --generate-hashes --universal \
#     --python-version 3.11 -o requirements/dev.txt
agentrust-trace>=0.5
agent-manifest>=0.12.0
cryptography>=50.0,<51.0
pyyaml>=6.0
httpx>=0.27
anyio>=4.0
starlette>=0.37
uvicorn>=0.29
click>=8.1
cedarpy>=4.0
rfc8785>=0.1.4
mcp>=1.28.1,<3.0
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
types-pyyaml
bandit[toml]>=1.7
pip-audit>=2.6
# Pinned rather than left to the runner's copy: dropping the old
# "pip install --upgrade setuptools" prefix would otherwise freeze whatever
# the image ships, and pip-audit rightly flags 79.0.1 for PYSEC-2026-3447.
setuptools>=83.0.0
